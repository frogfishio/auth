{
  "name": "@frogfish/auth",
  "version": "1.0.6",
  "description": "Kona Auth Service",
  "main": "gulpfile.js",
  "scripts": {
    "test": "gulp prepare-debug; tsc; mocha --exit --recursive build/debug/test/account",
    "watch": "gulp prepare-debug; tsc -w",
    "build": "gulp build",
    "clean-build": "gulp clean; gulp build",
    "dist": "npm version patch; gulp clean; gulp build; cd build/release; npm publish --access public; git push origin master"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/frogfishio/auth.git"
  },
  "author": "",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/frogfishio/auth/issues"
  },
  "homepage": "https://github.com/frogfishio/auth#readme",
  "dependencies": {
    "@frogfish/engine": "^1.0.18"
  },
  "devDependencies": {
    "del": "^5.1.0",
    "gulp": "^4.0.2",
    "gulp-typescript": "^5.0.1",
    "mkdirp": "^0.5.1",
    "typescript": "^3.6.3"
  }
}
